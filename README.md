# setup

Note that these scripts are tested only in Rocky Linux.

We recommend you install with following order.

```
# as root

gnome-gui.sh
nvidia1.sh
nvidia2.sh
nas.sh
flathub-apps.sh
```

```
# as a user

user.sh
```
